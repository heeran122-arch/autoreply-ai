# Dockerfiles and docker-compose for AutoReply AI

This file set includes Dockerfiles for the server and client and a docker-compose.yml so you can run the full stack locally (Postgres, server, and client/nginx) without installing Node or Postgres locally.

Important: You still need to provide Google OAuth credentials and an AI API key in the .env file before starting. See .env.example.
